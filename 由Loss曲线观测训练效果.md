| 特征                                                         | 可能的原因                                                   | 曲线示例                                                     |
| :----------------------------------------------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **Loss 持续不降：**训练过程中 Loss 始终保持高位，无下降趋势。 | 学习率（Learning Rate）过小：参数更新幅度不足，模型难以优化，难以学到东西。可以尝试加大学习率数据无规律/噪声大：数据中存在大量异常值/缺失值 | ![image-20250714121008818](由Loss曲线观测训练效果.assets/image-20250714121008818.png) |
| **Loss 持续下降但不收敛：**Loss 一直在下降，但收敛速度极慢，训练结束时仍未达到理想水平 | 训练轮数太小，不收敛的情况下可以一直增大训练轮数（不要超过10），反之提前收敛可以减小训练轮数。 | ![image-20250714121040635](由Loss曲线观测训练效果.assets/image-20250714121040635.png) |
| **Loss 震荡波动：**Loss 曲线频繁上下起伏，无稳定下降趋势。   | 学习率（Learning Rate）过大：参数更新幅度过大，导致优化路径震荡。  批量大小（Batch Size）过小：单次更新的梯度噪声大，方向不稳定。 | ![image-20250714121054657](由Loss曲线观测训练效果.assets/image-20250714121054657.png) |
| **Loss 先降后停/趋于稳定：**训练初期 Loss 下降明显，后期停滞在某一值附近。 | 学习率衰减至过低：模型进入“学习瓶颈”，无法进一步优化。  已收敛到局部最优：当前参数下 Loss 无法继续降低。  过拟合：模型在训练集上表现良好，但泛化能力不足（需结合验证集判断）。 | ![image-20250714121108672](由Loss曲线观测训练效果.assets/image-20250714121108672.png) |
| **Loss 下降后反弹/上升：**Loss 下降到一定程度后突然回升。    | 学习率突变：如突然增大学习率导致参数更新“冲过”最优解。  数据异常：训练集中混入噪声或异常样本。  模型结构问题：如网络层数过多导致过拟合，或层间参数不匹配。 | ![image-20250714121120467](由Loss曲线观测训练效果.assets/image-20250714121120467.png) |
| **理想形态**：Loss 逐步下降并趋于平缓                        | 各种参数和数据集都挺好（但并不代表模型最终的效果就好）       | ![image-20250714121130755](由Loss曲线观测训练效果.assets/image-20250714121130755.png) |

| 特征                                                         | 可能的原因                                                   | 曲线示例                                                     |
| :----------------------------------------------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **Train LOSS 下降，Eval LOSS 上升：**模型在训练集上表现越来越好，但验证集损失反升。 | **过拟合：**模型过度记忆训练数据细节，泛化能力不足。一般就是数据集质量太差了。 | ![image-20250714121244262](由Loss曲线观测训练效果.assets/image-20250714121244262.png) |
| **Train LOSS 和 Eval LOSS 均高位：**训练损失和验证损失都居高不下 | **欠拟合：**和上面一样的原因，模型没学到东西，需要调大学习率、Lora 秩，或者增强数据集质量。 | ![image-20250714121253821](由Loss曲线观测训练效果.assets/image-20250714121253821.png) |
| **Train 高 Eval 低：**训练损失居高不下，验证损失正常下降     | **数据分布问题**：验证集数据比训练集简单，或两者分布差异大，确保训练集与验证集分布一致，重新划分数据集。 | ![image-20250714121301965](由Loss曲线观测训练效果.assets/image-20250714121301965.png) |
| **Eval 降得快：**验证损失下降明显快于训练损失                | **数据分布问题**：验证集可能包含更多“易例”，或训练集存在难例样本未被模型学习。 可以在训练集中增加难例样本，或减少验证集大小。 | ![image-20250714121316759](由Loss曲线观测训练效果.assets/image-20250714121316759.png) |
| **理想形态**：Train LOSS 和 Eval LOSS 同步下降               | 模型在有效学习，且未出现过拟合，可继续训练直至收敛。         | ![image-20250714121326402](由Loss曲线观测训练效果.assets/image-20250714121326402.png) |